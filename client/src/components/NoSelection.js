import "./NoSelection.css";

const NoSelection = () => (
  <section className="App-noselection-wrapper">
    <div className="App-noselection">
      <p>
        There is no selected person, select a person from the list of people or
        type a person's ID in the search box to view the details.
      </p>
    </div>
  </section>
);

export default NoSelection;
